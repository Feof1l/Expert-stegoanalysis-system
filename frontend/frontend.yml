services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: frontend_app
    restart: unless-stopped
    ports:
      - '3000:80'
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
